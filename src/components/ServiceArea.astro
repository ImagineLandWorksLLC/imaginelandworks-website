---
import { faLocationDot } from "@fortawesome/free-solid-svg-icons";
import type { ServiceCity } from "@data/serviceArea";

interface Props {
  cities: ServiceCity[];
  ctaLabel?: string;
  ctaHref?: string;
  strokeColor?: string;
  fillColor?: string;
  width?: number;
  height?: number;
}

const {
  cities,
  ctaLabel,
  ctaHref,
  strokeColor = "var(--color-primary)",
  fillColor = "var(--color-base-300)",
  width,
  height,
} = Astro.props;

const [w, h, , , path] = faLocationDot.icon;
const pinD = Array.isArray(path) ? path.join(" ") : path;
const pinSvg = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 ${w} ${h}" fill="currentColor" class="size-4 shrink-0 mt-0.5" aria-hidden="true"><path d="${pinD}" /></svg>`;
---

<section class="bg-base-200 py-20">
  <div class="container mx-auto px-6">
    <div class="flex flex-col lg:flex-row items-center gap-12 lg:gap-16">

      <!-- Map (left) — temporarily commented out -->
      <!--
      <div class="w-full lg:w-1/2 flex justify-center">
        <div class="card bg-base-100 border border-base-300 shadow-md rounded-2xl overflow-hidden w-full max-w-md">
          <div class="card-body p-6">

            <svg
              viewBox="0 0 400 400"
              xmlns="http://www.w3.org/2000/svg"
              aria-label="Map showing Imagine Land Works service area in central Florida"
              role="img"
              class={width || height ? undefined : "w-full h-auto"}
              width={width}
              height={height}
            >
              <defs>
                <pattern id="dots" x="0" y="0" width="16" height="16" patternUnits="userSpaceOnUse">
                  <circle cx="2" cy="2" r="1.2" fill="var(--color-base-content)" opacity="0.12" />
                </pattern>
                <filter id="glow" x="-30%" y="-30%" width="160%" height="160%">
                  <feGaussianBlur stdDeviation="6" result="blur" />
                  <feMerge>
                    <feMergeNode in="blur" />
                    <feMergeNode in="SourceGraphic" />
                  </feMerge>
                </filter>
              </defs>

              <rect width="400" height="400" fill="url(#dots)" />

              <path
                d="M310 40h-40l-20 10-30-5-25 10-20 20-15 5-10 15 5 20-10 15-20 10-10 20 5 20-10 15 5 20 20 10 10 20 20 10 10 20 20 10 10 20 15 10 10 20 20 10 10-10 5-20 10-10 5-20 10-10 5-20 10-10 5-20 10-10 5-20 10-10 5-20-10-20-20-10-10-20-20-10-10-20z"
                fill={fillColor}
                stroke={strokeColor}
                stroke-width="4"
                stroke-linejoin="round"
                stroke-linecap="round"
              />

              <ellipse cx="192" cy="170" rx="38" ry="24" fill="var(--color-secondary)" opacity="0.18" filter="url(#glow)" />
              <ellipse cx="192" cy="170" rx="38" ry="24" fill="none" stroke="var(--color-secondary)" stroke-width="2" opacity="0.5" stroke-dasharray="6 4" />

              <circle cx="190" cy="173" r="7" fill="var(--color-accent)" stroke="var(--color-base-100)" stroke-width="2" />
              <circle cx="205" cy="160" r="5" fill="var(--color-primary)" stroke="var(--color-base-100)" stroke-width="2" />
              <circle cx="218" cy="173" r="5" fill="var(--color-primary)" stroke="var(--color-base-100)" stroke-width="2" />
              <circle cx="188" cy="188" r="5" fill="var(--color-primary)" stroke="var(--color-base-100)" stroke-width="2" />
              <circle cx="192" cy="148" r="5" fill="var(--color-primary)" stroke="var(--color-base-100)" stroke-width="2" />
              <circle cx="172" cy="184" r="5" fill="var(--color-primary)" stroke="var(--color-base-100)" stroke-width="2" />
              <circle cx="154" cy="162" r="5" fill="var(--color-primary)" stroke="var(--color-base-100)" stroke-width="2" />
              <circle cx="214" cy="188" r="5" fill="var(--color-primary)" stroke="var(--color-base-100)" stroke-width="2" />

              <text x="200" y="175" font-size="11" fill="var(--color-primary)" font-family="sans-serif" font-weight="700">Lakeland</text>

              <g transform="translate(14, 365)">
                <circle cx="7" cy="5" r="6" fill="var(--color-accent)" stroke="var(--color-base-100)" stroke-width="1.5" />
                <text x="18" y="9" font-size="10" fill="var(--color-base-content)" font-family="sans-serif">Primary city</text>
                <circle cx="115" cy="5" r="5" fill="var(--color-primary)" stroke="var(--color-base-100)" stroke-width="1.5" />
                <text x="126" y="9" font-size="10" fill="var(--color-base-content)" font-family="sans-serif">Service city</text>
              </g>
            </svg>

          </div>
        </div>
      </div>
      -->

      <!-- Content (right) -->
      <div class="w-full lg:w-1/2 space-y-8">

        <!-- Header -->
        <div>
          <p class="text-secondary font-heading text-xl uppercase tracking-widest mb-2">
            Service Area
          </p>
          <h2 class="text-primary font-heading leading-tight">
            Proudly Serving Lakeland,<br />Polk County & Surrounding Areas.
          </h2>
          <p class="text-base-content/70 mt-4 leading-relaxed">
            Based in Lakeland, we run crews throughout Polk County and into neighboring
            counties. If you're within driving distance of central Florida, there's a
            good chance we can get to you.
          </p>
        </div>

        <!-- City list -->
        <ul class="grid grid-cols-1 sm:grid-cols-2 gap-3">
          {cities.map((city) => (
            <li class="flex items-start gap-2.5">
              <span class="text-accent mt-0.5">
                <Fragment set:html={pinSvg} />
              </span>
              <div class="leading-tight">
                <span class="font-heading text-primary text-base">{city.name}</span>
                {city.county && (
                  <span class="block text-base-content/50 text-xs uppercase tracking-wider">
                    {city.county}
                  </span>
                )}
              </div>
            </li>
          ))}
        </ul>

        <!-- CTA -->
        {ctaLabel && ctaHref && (
          <div class="pt-2">
            <a href={ctaHref} class="btn btn-outline btn-primary">
              {ctaLabel}
            </a>
          </div>
        )}

      </div>

    </div>
  </div>
</section>
